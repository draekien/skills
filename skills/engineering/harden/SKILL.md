---
name: harden
description: DEPRECATED — this skill now ships in the quality-skills plugin. Install that plugin and use harden instead.
argument-hint: "[--mode inline|subagents] [target]"
disable-model-invocation: true
---

# Deprecated — moved to the `quality-skills` plugin

The copy here does nothing.

Tell the user:

1. This skill has moved and the copy here does nothing.
2. Install it, either route:
   - `/plugin install quality-skills@draekien-skills` in a harness with plugin support
   - `npx skills add draekien/skills --skill "harden"` anywhere else
3. Re-run the request; the moved skill picks it up.

Then stop. Do not attempt the work from this file.
