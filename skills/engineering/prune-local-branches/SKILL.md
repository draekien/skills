---
name: prune-local-branches
description: DEPRECATED — this skill now ships in the version-control-skills plugin. Install that plugin and use prune-local-branches instead.
argument-hint: "[--base branch]"
disable-model-invocation: true
---

# Deprecated — moved to the `version-control-skills` plugin

The copy here does nothing.

Tell the user:

1. This skill has moved and the copy here does nothing.
2. Install it, either route:
   - `/plugin install version-control-skills@draekien-skills` in a harness with plugin support
   - `npx skills add draekien/skills --skill "prune-local-branches"` anywhere else
3. Re-run the request; the moved skill picks it up.

Then stop. Do not attempt the work from this file.
