---
name: init-agent-docs
description: DEPRECATED — this skill moved to the context-management bucket as context-management/init-agent-docs. Install the context-management-skills plugin and use that skill instead.
disable-model-invocation: true
---

# Deprecated — moved to the context-management bucket

This skill now lives at `skills/context-management/init-agent-docs`, in the `context-management` bucket. Its `assets/` templates moved with it.

Tell the user:

1. This skill has moved and the copy here does nothing.
2. Install the new bucket: `/plugin install context-management-skills@draekien-skills`.
3. Re-run the request; the moved skill picks it up.

Then stop. Do not attempt the work from this file.
