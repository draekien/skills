---
name: module-design
description: DEPRECATED — this skill now ships in the software-design-skills plugin. Install that plugin and use module-design instead.
argument-hint: "[--mode design|audit] [module-or-file]"
disable-model-invocation: true
---

# Deprecated — moved to the `software-design-skills` plugin

The copy here does nothing.

Tell the user:

1. This skill has moved and the copy here does nothing.
2. Install it, either route:
   - `/plugin install software-design-skills@draekien-skills` in a harness with plugin support
   - `npx skills add draekien/skills --skill "module-design"` anywhere else
3. Re-run the request; the moved skill picks it up.

Then stop. Do not attempt the work from this file.
