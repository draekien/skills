---
name: extract-llms-txt
description: DEPRECATED — this skill now ships in the context-management-skills plugin. Install that plugin and use extract-llms-txt instead.
disable-model-invocation: true
---

# Deprecated — moved to the `context-management-skills` plugin

The copy here does nothing.

Tell the user:

1. This skill has moved and the copy here does nothing.
2. Install it, either route:
   - `/plugin install context-management-skills@draekien-skills` in a harness with plugin support
   - `npx skills add draekien/skills --skill "extract-llms-txt"` anywhere else
3. Re-run the request; the moved skill picks it up.

Then stop. Do not attempt the work from this file.
